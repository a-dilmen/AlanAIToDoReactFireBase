// Login to firebase via link https://firebase.google.com/ and create a project 
// go to console 
// on left sidebar at the top click on settings icon 
// click on project settings
// in the general tab you can find sdk setup and configurration
// run npm install firebase if you did not already
// copy paste config and credentials snippet that is already generated for your project
// import Fire Store via    import { getFirestore } from 'firebase/firestore'
// export databse via       export const database = getFirestore(app)
// also add export to app declaration  as export const app = initializeApp(firebaseConfig);
